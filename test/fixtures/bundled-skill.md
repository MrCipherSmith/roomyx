# startup-room (test fixture placeholder)

This is a placeholder used only by roomyx-installer's own tests. It is NOT
the real `startup-room` skill content — see docs/requirements/roomyx-installer/
decisions.md D-02: authoring/syncing the real bundled skill content is an
explicit, separate, later step, not part of this test fixture.
