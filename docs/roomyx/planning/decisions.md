# Decisions Registry
<!-- Append-only. Orchestrator and consistency-checker read this file. -->

| ID | Phase | Decision | Value | Rationale |
|----|-------|----------|-------|-----------|
| D_mode | 0 | Project mode | task_in_project | Existing published package at /home/altsay/roomyx; work is a defect backlog, not a new build |
| D_source_of_truth | 0 | Findings are inputs, not outputs | docs/roomyx/improvement-backlog.md | 15 defects already reproduced against published 0.4.0 and ranked by a five-participant room; the pipeline plans them, it does not re-derive them |
| D_ranking_axis | 0 | Ranked order ≠ build order | Both must be stated | Room's explicit constraint: an implementer reading the ranked five as a work queue starts with a 3-day render rewrite for rooms they have no supported way to create |
| D_decision_gates | 0 | Two items are gated on amendments to recorded decisions | R3 → D-01 (roomyx), R5/S2 → D-06 (roomyx) | Amending a recorded decision is the owner's act, not an implementer's; the room ruled R3 out of the ranked five for exactly this reason |
| D_domain | 0 | Business domain | Developer tooling — MCP server + TUI for multi-agent startup-room sessions | From discovery |
| D_audience | 0 | Who this serves | Solo/small-team devs running startup-room, **plus LLM agents as first-class CLI/MCP consumers** | Reframes R8 (`--help` is how an agent learns a CLI) and R10 (3.8 MB block is a context window, not CPU) |
| D_scale_estimate | 0 | Scale | Personal / solo-maintainer OSS, public on npm, unknown install base | Blast radius of surface changes is a medium-confidence assumption; no download data offline |
| D_approver | 0 | Named approver for decision amendments | MrCipherSmith — sole npm scope holder, 22/23 commits | Gate is procedural, not organisational: owner == implementer, so only the plan's definition-of-done can enforce it |
| D_compat_event | 0 | S1 + R8 + R4 change published surface | Needs a version and README decision, not just code | `targetPath` removal, `serve` default 4319→0, `bin` shape |
| D_success_metric | 0 | What "done" means for this project | Documented behaviour and shipped behaviour agree | Defect remediation on shipped software, not features delivered |
| D_no_reestimate | 0 | Sizes and scope are fixed inputs | Do not re-estimate; do not promote the below-line four (R9 excepted as a ride-along with R2); rubric repair out of scope | The room argued these; the pipeline plans them |
| D_sdk_guard_reaches | 0 | S2's guard is a constructor option, not a hand-rolled check | `StreamableHTTPServerTransport` is a thin wrapper forwarding options to `WebStandardStreamableHTTPServerTransport`; guard at `webStandardStreamableHttp.js:141` | Supersedes an intermediate correction that read a grep miss as an absence |
| D_sdk_floor | 0 | S2 must raise the `@modelcontextprotocol/sdk` floor | Declared `^1.0.0`, resolved 1.30.0 | An older satisfying 1.x may predate the option forwarding, so a fresh install would get a guard that silently does nothing |
| D_transport_duplication | 0 | Every transport fix lands twice | `src/server/serve.ts` and `src/mcp-management/server.ts` are ~90% duplicated | Either apply twice or extract the shared shape first |
| D_no_mocking | 0 | Test convention | Zero mocking libraries — real servers, real SDK clients, real `Bun.spawn` subprocesses | New tests must follow |
| D_opentui_primitives | 0 | R1 and R12 need no hand-rolled wrapping or scroll math | `@opentui/core@0.5.11` ships `wrapMode`/`truncate` and `scrollBy`/`scrollTop`/`stickyScroll` setters | Reduces R1's risk; the work is the state machine, not the primitives |
| D_problem_frame | 1 | Nine problems P1–P9 | Every one a written promise the shipped code breaks — not a capability gap | Defect remediation, not feature delivery |
| D_r9_ride_along | 1 | R9 splits | Deregister-order fix ships in R2's diff; the SIGTERM-drain half stays below the line | The backlog said both "below the line" and "rides along with R2"; splitting resolves it without promoting or dropping |
| D_personas | 1 | Two co-primary users | Human operator + LLM agent driving CLI/MCP; owner-as-approver; MCP-embedder secondary; one adversary (a web page, P6 only) | Several findings only look serious through the second lens |
| D_gate_is_dod | 1 | The amendment gate exists only as a blocking definition-of-done | owner == implementer == approver | Nothing organisational enforces it |
| D_sdk_guard_superseded | 1 | ~~R5's "guard the SDK already ships" wording is dead~~ | **RETRACTED — see D_sdk_guard_reaches.** Recorded from a stale read of the backlog; the guard does reach the transport | Append-only registry: the row stays, marked wrong, so the reversal is traceable |
| D_deferred_to_owner | 1 | Four questions Phase 6 must carry to the owner, not resolve | release packaging shape; smoke-test scope; S1's changelog line with no CHANGELOG.md; the R3/R8 `seed.ts` collision | Low-confidence inputs; deciding them silently would bury a compatibility event |
| D_level | 2 | Project level | Published solo-maintainer 0.x dev tool — pet-scale on team/budget/SLA, production-scale on distribution/reversibility | Light process on most fixes; heavy on the security surface, the published surface, and the two amendments |
| D_sdk_floor_ruling | 2 | **OWNER RULING: `@modelcontextprotocol/sdk` floor is `>=1.25.0`** | Option A | Measured, not guessed: guard enforced ≥1.13.3, lenient-Origin ≥1.24.0, option forwarding ≥1.25.0. 1.25.0 is the only range where the tested path equals the shipped path |
| D_sdk_floor_rejected | 2 | `>=1.13.3` rejected outright | Pre-1.24.0 rejects requests with no `Origin` header — roomyx's own TUI client and `isRoomLive()` send none, so the product would 403 itself on untested installs | The naive "where the guard first exists" answer is actively harmful |
| D_guard_scope | 2 | The guard stops browsers, not local processes | A local caller sends no `Origin` and passes the lenient check | Consistent with D-06's threat model; must not be described as authentication |
| D_s4_before_s2 | 2 | Ordering constraint | S4 (`--frozen-lockfile` at release) must not ship after S2 | A raised floor is meaningless while the release job resolves freely under `--provenance` — provenance would attest a tree nobody tested |
| D_no_build_step_unrecorded | 2 | R4 needs a doc edit, not a carve-out | "No build step" is README prose; grep of both registries for build/transpile/shebang/bun returns zero | Confirms the Phase 0 finding independently |
| D_transport_extraction | 3 | **OWNER RULING: extract the shared transport once, first** | `src/server/http-transport.ts`, its own commit inside S2's PR | Measured: the two bodies differ in 4 hunks; 7 test files use these servers and **0** import internals, so an extraction preserving `serve()`/`serveManagement()` signatures edits zero tests. The "four test suites" objection was mine and did not survive a grep |
| D-07_new | 3 | **OWNER RULING: record D-07 (единый HTTP-транспорт)** | `docs/roomyx/decisions.md`, in the extraction commit | A structural decision that outlives the PR that made it |
| D_scroll_keys | 3 | **OWNER RULING: PgUp/PgDn/Home/End scroll the transcript** | ↑/↓ stay with the roster; `ChatView.handleTranscriptKey()`, scroll stays private | `ScrollBoxRenderable.handleKeyPress` already implements these; the work is forwarding, not implementing |
| D_r1_no_state_machine | 3 | `stickyScroll` suspend/resume is already correct | Measured by rendering, not read: scrolled to 13, +6 messages, position and rows identical; `End` re-engaged | The promise at `testing-story.md:171` is already kept by the library |
| D_r1_seq1_root_cause | 3 | The seq-1 bug is the horizontal scrollbar, not sticky-bottom | It eats one viewport row, so `maxScrollTop`=1 on an under-filled pane and sticky-bottom drops seq 1. Fix: `horizontalScrollbarOptions: { visible: false }` at `chat-view.ts:36` | Found by bisection; independently reproduced by the orchestrator — one message renders invisible by default and visible with the option |
| D_r1_reprice | 3 | **OWNER RULING: accept the reduced size for R1's scroll half** | Wrapping, `kind`/`in_reply_to`, empty/error states and the failing test are unchanged | The `D_no_reestimate` exception, invoked with measured evidence rather than opinion |
| D_guard_allowlist | 3 | Allowlists are built lazily from the bound port, inside `createSession()` | A port-less `allowedHosts` returns 403 **to roomyx's own client**; both `allowedHosts` and `allowedOrigins` are required — they defend different attacks | Verified live |
| D_launcher_import_meta_main | 5 | R4 must preserve `import.meta.main === false` for `client/index.ts` under `await import()` | Asserted at effect level: `roomyx client` reaches `runClient()` exactly once, through the launcher | V-001. The constraint had named `import.meta.dir`, which does not appear in that file; it guarded a property that does not exist and was silent on the one a launcher actually breaks — a launcher that spawns or re-execs that entry runs the client twice |
| D_s3_introduces_syncskill_edge | 5 | `init.ts` has no `syncSkill` precedent to follow | It imports only `node:fs` and `node:path`; S3 creates that dependency edge | V-002. Several artefacts sent an implementer to a reference implementation that is not in the file — a forward reference to the very work S3 is |
| D_r11_key_substitution | 5 | `Home`/`End` replace `Ctrl-U`/`Ctrl-D` | `ScrollBoxRenderable.handleKeyPress` implements all four ruled keys natively; half-pages need their own delta arithmetic — the hand-rolled scroll math the ruling exists to avoid | W-007. Recorded so the substitution does not read as silent scope loss |
| D_backlog_r5_wording | 5 | `improvement-backlog.md` R5 intro corrected by the orchestrator | It still asserted "the SDK does not provide it for this transport" 250 lines after its own retraction | V-004. My text, my error, fixed in place rather than deferred |
| D_anchor_rule | 5 | Proposed checker rule for future rooms | Constraints citing line anchors are diffed against their source, never paraphrased | The V-001 truncation happened in compression from a `stack-decision.md` line that was correct |
| D_leak_criterion | 5 | **OWNER RULING: the leak is proven by the session map, not by RSS** | The map is empty after N connect/close cycles; `ServeHandle` gains a session count | Closes V-003. A leak is by nature an entry that was not removed, not bytes: a map size is a binary predicate, while RSS on a GC runtime gives false results in both directions. Neither the checker nor the PRD proposed this — they were arguing about the numbers for the wrong observable |
| D_reaper_shape | 5 | **OWNER RULING: explicit termination plus an idle sweep** | `terminateSession()` on the client side, and a server-side sweep for sessions nothing has touched | Explicit termination alone fixes only well-behaved clients; a hard-killed terminal never sends DELETE, and the item exists because of the clients that do not say goodbye. No adversary required |
| D_release_gate | 5 | **OWNER RULING: strengthen the smoke test** | Remove `\|\| true`, assert the exit code and the output content at `release.yml:91` | One line turns a decorative check into a real one. Answers both US-015 AC2 and the deferred smoke-test question at once — the gate and the mechanism were the same thing |
| D_release_shape | 6 | **OWNER RULING: 0.5.0 first, then 0.6.0** | 0.5.0 = S1 (`targetPath`) + S2 (guard + floor) + S4 (`--frozen-lockfile`); 0.6.0 = R8, R4, R3; render work ships as patches | A fast 0.4.1 was rejected as formally wrong: removing a field from the network tool breaks embedders, and under 0.x a breaking change belongs in the minor position. One combined release would hold the hole closed hostage to the longest work |
| D_changelog | 6 | **OWNER RULING: create `CHANGELOG.md`** | Gives S1 its own line, as the room required | Generated release notes answer "what changed" but not "why" or "what to do if you depended on this" — and 0.5.0 is precisely a release that breaks something for embedders |
| D_seed_collision | 6 | **OWNER RULING: R3 before R8** | R3 absorbs `seed.ts`; R8 then unifies two parsers instead of three | Matches the already-ruled build order. The reverse means unifying a file that disappears immediately afterwards |
| D_release_gate_mechanism | 6 | **OWNER RULING: the gate lands in 0.5.0 on `--version`, and gains `--help` in 0.6.0** | `release.yml:91` drops `\|\| true`, asserts exit 0 and that the printed version equals the tag; the `--help` assertion is added once R8 makes `--help` exit 0 | Overrides the planner's proposal to defer the whole gate. It reasoned inside "the smoke test is about `--help`" — but `--version` exists, exits 0 today, and checking it against the tag is *stricter* than what the step does now. The ruling is executed in the part that is already possible rather than postponed whole |
| D_planner_five | 6 | **OWNER RULING: ratify all five planner decisions** | N=200 with three assertions; patch-stream contents; G6 dated at v0.6.3; PKG-8 raised to MUST; estimate bands as presentation | N=200 is not arbitrary — it is the number Omar's +18 MB measurement used, so the results stay comparable. The third assertion exists specifically to fail a counter that passes vacuously |
| D_commit_shape | 6 | **OWNER RULING: commit to main, split by meaning** | Three separate commits: the room's output; the documentation package; the walkthrough correction | Different work with different reasons should not arrive as one blob |
| D_start_m1 | 6 | **OWNER RULING: begin implementation with milestone 0.5.0 in full** | S1 + transport extraction + S2 + S4 + the D-06 and D-07 records + CHANGELOG + the release gate | The first item is minutes of work against a live unauthenticated arbitrary-path write |
