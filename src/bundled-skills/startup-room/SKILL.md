# startup-room (roomyx-bundled placeholder)

**This is a placeholder, not the real, live `startup-room` skill content.**

Authoring the actual auto-launch-enabled skill text — the version that will
eventually replace `~/.claude/skills/startup-room/SKILL.md` — is explicitly
out of scope for `roomyx-installer` flow 003 (see
`docs/requirements/roomyx-installer/description.md` and `decisions.md` D-02).
This file exists only so `roomyx init` and `roomyx skills sync` have a real
file to stage/copy/diff while the mechanism itself is built and tested.

Do not sync this placeholder over a real skill file.
