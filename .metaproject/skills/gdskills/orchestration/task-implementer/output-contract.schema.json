{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://keryx.local/schemas/task-implementer/output-contract.schema.json",
  "title": "Task Implementer Output Contract",
  "description": "JSON result task-implementer writes in Phase 6.1. Registered as contract `task-implementer-output`: validate a result with `keryx skills contracts validate <result.json> --schema task-implementer-output`.",
  "type": "object",
  "additionalProperties": false,
  "required": ["task_id", "task_name", "task_type", "status"],
  "properties": {
    "task_id": { "type": "string", "pattern": "^(?:task|fix)-[0-9]+$" },
    "task_name": { "type": "string" },
    "task_type": {
      "type": "string",
      "enum": ["ui_component", "store_logic", "service_api", "refactoring", "fix", "mixed"]
    },
    "status": {
      "type": "string",
      "enum": ["success", "partial", "failed"],
      "description": "success: all criteria met, all verifications pass; partial: some criteria met or verification failures; failed: critical blockers"
    },
    "description": { "type": "string", "description": "What was implemented" },
    "files_modified": { "type": "array", "items": { "type": "string" } },
    "files_created": { "type": "array", "items": { "type": "string" } },
    "files_deleted": { "type": "array", "items": { "type": "string" } },
    "commits": { "type": "array", "items": { "type": "string" } },
    "lint_result": {
      "type": "string",
      "description": "pass OR 'N errors: <details>'"
    },
    "type_check_result": {
      "type": "string",
      "description": "pass OR 'N errors: <details>'"
    },
    "test_result": {
      "type": "string",
      "description": "pass OR 'N passed, M failed: <details>' OR 'skipped'"
    },
    "story_result": {
      "type": "string",
      "description": "pass OR 'build error: <details>' OR 'not applicable'"
    },
    "acceptance_criteria_met": {
      "type": "string",
      "description": "all OR 'partial: <list of unmet criteria>' OR none"
    },
    "skill_drift": {
      "type": "string",
      "description": "Set from SKILL.md Phase 2.0b: 'none' | 'stale: <module>/<skill> — <what diverged>' | 'missing: <module> should have a project-skill'. SKILL.md has required this field since project-skill verification was added; this object is `additionalProperties: false`, so until it was declared here a compliant result was REFUSED by its own contract."
    },
    "notes": {
      "type": "string",
      "description": "Warnings, blockers, or additional context"
    }
  }
}
